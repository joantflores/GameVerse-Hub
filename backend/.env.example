# Example .env (DO NOT STORE real secrets in the repo)
# Replace the values in Render/Environment Variables

# Twitch / IGDB credentials
TWITCH_CLIENT_ID=your_twitch_client_id
TWITCH_CLIENT_SECRET=your_twitch_client_secret

# SMTP (Gmail) credentials - use App Password for Gmail
SMTP_USER=your_email@example.com
SMTP_PASS=your_app_password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
EMAIL_FROM="GameVerse Hub <no-reply@yourdomain.com>"

# Notes:
# - DO NOT commit real secrets to git.
# - Set real values in your cloud host (Render) Environment Variables.
