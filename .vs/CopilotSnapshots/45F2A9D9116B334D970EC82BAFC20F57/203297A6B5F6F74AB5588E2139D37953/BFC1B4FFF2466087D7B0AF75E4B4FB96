# Twitch / IGDB credentials
TWITCH_CLIENT_ID=your_twitch_client_id
TWITCH_CLIENT_SECRET=your_twitch_client_secret

# SMTP (Gmail) credentials - use App Password for Gmail
SMTP_USER=your@gmail.com
SMTP_PASS=your_app_password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
EMAIL_FROM="GameVerse Hub <no-reply@yourdomain.com>"

# Server port
PORT=3000

# Notes:
# - Copy this file to `.env` and fill the real secrets locally. Do NOT commit your real `.env` to git.
# - In your cloud host (Render/Heroku/etc) set these as environment variables in the service settings.
